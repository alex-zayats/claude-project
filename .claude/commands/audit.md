This is command to update the vulnerabilities in the project.

1. Run "npm audit" command to find vulnerable installed packages
2. Run "npm audit fix" to apply updates
3. Run tests to verify the updates didn't break anything
